apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: controller-service
  labels:
    app: api-gateway
    service: controller
spec:
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: api-gateway
  type: ClusterIP